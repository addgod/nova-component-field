{
    "name": "addgod/component-field",
    "description": "A Laravel nova field, that can be used to generate json, based on any nova compatible fields.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "Addgod\\ComponentField\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Addgod\\ComponentField\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
